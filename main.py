import threading

# Функція для тайм-ауту
def timeout():
    print("\n⏰ Час вийшов!")
    exit()

# Введення відсотка, який буде відніматися
x = input("Введіть відсотки, які будуть відніматися: ")
try:
    x = float(x)
    # Формула для розрахунку скільки відсотків треба додати
    y = (x * 0.01) / (0.01 - x * 0.0001)
    print(f"👉 Щоб після віднімання {x:.2f}% залишилась сума як була, потрібно попередньо додати: {y:.2f}%")
except ValueError:
    print("❌ Помилка: введіть число")
    exit()

# Запуск таймера на 15 секунд
time = threading.Timer(15.0, timeout)
time.start()

# Введення суми
s = input("Введіть суму: ")
time.cancel()

# Розрахунок нової суми
try:
    a = float(s)
    result = a + 0.01 * a * y
    print(f"\n📌 Початкова сума: {a:.2f} грн")
    print(f"➕ Необхідно додати {y:.2f}% → {result:.2f} грн")
except ValueError:
    print("❌ Помилка: введіть число")
    exit()
